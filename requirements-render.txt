# Lightweight profile for Render's free tier (512 MB RAM).
# Excludes the heavy ML stack (torch, transformers, insightface,
# onnxruntime, opencv) — those imports are lazy in app/ml/*, so the API
# runs fully except: local BLIP-2 scene description (set
# USE_LOCAL_VLM=false) and server-side face-embedding extraction on
# /faces/enrol. /faces/match works (pure numpy on stored embeddings).
fastapi==0.111.0
uvicorn[standard]==0.29.0
gunicorn==22.0.0
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
alembic==1.13.1
pydantic[email]==2.7.1
pydantic-settings==2.3.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.9
Pillow==10.3.0
numpy==1.26.4
redis==5.0.4
slowapi==0.1.9
httpx==0.27.0
cryptography==42.0.7
python-dotenv==1.0.1
huggingface_hub==0.23.2
